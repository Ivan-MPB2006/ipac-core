# README — Sprint 02 Project Resource Replacement

Этот спринт создаёт ресурсный пакет v0.2 для Wise Supervisor (Мудрого Супервизора).

Сначала выполнить `RUN_ME_01_PLACE_TO_VAULT.ps1`, затем после проверки лога — `RUN_ME_02_COMMIT_TO_GIT.ps1`.

`git push` выполняется отдельным контрольным шагом после проверки commit log (журнала Git-проводки).
