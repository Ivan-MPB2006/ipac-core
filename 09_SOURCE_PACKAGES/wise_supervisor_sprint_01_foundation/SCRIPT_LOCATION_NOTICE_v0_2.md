# SCRIPT LOCATION NOTICE v0.2

Статус: candidate / review_input (кандидат / вход для рассмотрения)

В версии пакета v0.2 управляющие скрипты размещены в корне распакованной папки пакета:

```text
RUN_ME_01_PLACE_TO_VAULT.ps1
RUN_ME_02_COMMIT_TO_GIT.ps1
```

Они не спрятаны в глубину каталога, чтобы оператор сразу видел порядок запуска.

Этот файл размещён в Vault Drop (папке переноса в хранилище) как след изменения упаковки пакета.
