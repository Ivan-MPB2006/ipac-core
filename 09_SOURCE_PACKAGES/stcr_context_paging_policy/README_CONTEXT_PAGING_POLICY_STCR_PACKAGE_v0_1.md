# README — Context Paging Policy STCR Package v0.1
## README пакета политики контекстной подкачки STCR

Статус: **candidate (кандидат)**, **not canon (не канон)**.

Этот пакет создаёт P0-3 документ по рекомендациям эксперта: Context Paging Policy (политика контекстной подкачки) для Semantic Thread Compaction and Rehydration (смысловой свёртки треда и повторного развёртывания).

Короткий код пакета:

```text
STCR_CPP
```

Git commit (Git-проводка) не выполняется автоматически.
