# Process Regulation (процессное положение)
## Supervisor Mode Switching and Maneuver Preservation

```yaml
status: candidate (кандидат)
canon_status: not_canon (не канон)
promotion_authorized: false
git_commit_authorized: false
human_approval_required: true
```

```text
Работать максимально формализованно там, где можно.
Переходить к более смысловым режимам там, где формализация перестаёт попадать в реальность.
```

Нормальный контур:

```text
Algorithm (алгоритм)
→ Template (шаблон)
→ Framework (фреймворк)
→ Search Design (поисковое проектирование)
→ Human-AI Cognitive Aggregate (человеко-ИИ когнитивный агрегат)
```

Обратный контур стабилизации:

```text
Human-AI Cognitive Aggregate (человеко-ИИ когнитивный агрегат)
→ Search Design (поисковое проектирование)
→ Framework (фреймворк)
→ Template (шаблон)
→ Algorithm (алгоритм)
```
