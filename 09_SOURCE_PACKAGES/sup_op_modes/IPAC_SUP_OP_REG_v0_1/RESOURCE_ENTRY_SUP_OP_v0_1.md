# Resource Entry (ресурсная запись)

```yaml
status: candidate (кандидат)
canon_status: not_canon (не канон)
promotion_authorized: false
git_commit_authorized: false
human_approval_required: true
```

Resource (ресурс):

```text
IPAC_SUP_OP_PLACE_v0_1.zip
```

Назначение: короткий Windows-safe placement package (пакет размещения, безопасный для Windows) для разноса материалов по Obsidian Vault (хранилищу Obsidian).

Git Policy (политика Git): targeted Git add (точечное Git-добавление) только через `-GitAdd`; Git commit (Git-проводка) не выполняется.
